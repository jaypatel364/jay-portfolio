/**
 * @deprecated Prefer `@/settings` — this file re-exports for compatibility.
 * Edit identity / features / content under `settings/`.
 */
export {
  siteConfig,
  identity,
  features,
  content,
  type SiteConfig,
  type FAQCategory,
  type FAQItem,
  type BuildingItem,
  type LearningItem,
} from "@/settings";
